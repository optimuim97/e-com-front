# URL de l'API Laravel (sans /api à la fin)
# En développement, le proxy Vite redirige automatiquement /api → localhost:8000
# En production, indiquer l'URL complète du backend
VITE_API_URL=/api

# Exemple production:
# VITE_API_URL=https://api.rosabeauty.ci/api
